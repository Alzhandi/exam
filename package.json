{
  "name": "exam-prep-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix web run build",
    "vercel-build": "npm --prefix web install && npm --prefix web run build"
  },
  "dependencies": {
    "next": "^15.5.2"
  }
}
